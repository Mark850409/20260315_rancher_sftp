apiVersion: v1
kind: Service
metadata:
  name: sftp-service
  namespace: sftp
spec:
  type: NodePort
  ports:
    - port: 22
      targetPort: 22
      protocol: TCP
      name: 22tcp
      nodePort: 32222
  selector:
    app: sftp
